# anti-slop provenance

Source: https://github.com/dmmulroy/anti-slop at commit
`c44ef22ca116d0ba62a3ff663a0bd13a3f3fa40b`.

Installed production assets: `tools/oxlint/anti-slop/index.ts`,
`tools/oxlint/anti-slop/rules/`, `tools/oxlint/anti-slop/shared/`,
`tools/oxlint/anti-slop/effect/`, and `tools/oxlint/anti-slop/vendor/`.

The assets match that revision's `src/` after excluding upstream test files,
which the installer intentionally does not vendor. No local rule changes have
been made.
